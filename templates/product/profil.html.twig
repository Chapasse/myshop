{% extends "base.html.twig" %}

{% block body %}

{# <h1 class="text-center">Liste de vos commandes</h1>

<table class="table">
<thead>
    <tr>
        <th>Produit</th>
        <th>Quantité</th>
        <th>Prix total</th>
        <th>Date de la commande</th>
        <th>Etat</th>
    </tr>
</thead>
<tbody>
{% for item in commandes %}
    <tr>
        <td>{{ item.idproduct.titre }} - {{ item.idproduct.couleur }} - {{ item.idproduct.taille }} - {{ item.idproduct.collection }}</td>
        <td>{{ item.quantite }}</td>
        <td>{{ item.montant }}€</td>
        <td>{{ item.dateEnregistrement|date('d/m/Y à H:i:s') }}</</td>
        <td>{{ item.etat }}</td>
    </tr>
{% endfor %} #}
</tbody>
</table>

<h1>Votre profil</h1>
<div class="card mb-3 text-center">
  <h3 class="card-header"> pseudo : {{ app.user.pseudo }} </h3>
  <div class="card-body">
    <h5 class="card-title">{{ app.user.prenom }} {{ app.user.nom }}</h5>
    <h5 class="card-title"> {{ app.user.email }} </h5>
  </div>
  <div class="card-body">
  <p class="card-header"> genre : {{ app.user.civilite }} </p>
  </div>
  <div class="card-footer text-muted">
    (Membre depuis le {{ app.user.dateenregistrement|date("d/m/Y à H:i:s") }})
  </div>
</div>


<h2>Vos commandes</h2>
{% for commande in commandes %}
    {% if commande.idMembre.id == app.user.id %}
        <div class="card mb-3 text-center">
            <div class="card-header">
                <h3>Commande du {{commande.dateEnregistrement|date("d/m/Y à H:i:s")}}</h3>
            </div>
            <div class="card-body">
                <h4 class="card-title">{{ commande.quantite }} {{commande.idproduct.titre}}</h4>
                <div class="container">
                <p>taille : {{commande.idproduct.taille}} <br>couleur: {{commande.idproduct.couleur}}</p>
                <img src="{{commande.idproduct.photo}}" alt="Photo du vehicule à louer" width="50%">
                <p>prix total : {{commande.montant}} €</p>
                </div>
            </div>
        </div>    
    
    {% endif %}

{% endfor %}

{% endblock %}